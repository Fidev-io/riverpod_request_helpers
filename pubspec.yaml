name: riverpod_request_helpers
description: A helper for creating providers with riverpod to do async operations.
version: 0.0.2
repository: https://github.com/Fidev-io/riverpod_request_helpers

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^1.0.0
  freezed_annotation: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^1.0.0
  build_runner: any
